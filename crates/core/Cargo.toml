[package]
name = "cat-core"
version = "0.0.3"
edition = "2021"
license = "MIT"

[lib]
name = "cat_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
